{
    "tasks": [
        {
            "type": "shell",
            "label": "g++-5 build active file",
            "command": "/usr/bin/g++-5",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}