<UserControl x:Class="WpfRbTest2.PanelParts.ExecutionHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:ad="http://schemas.xceed.com/wpf/xaml/avalondock"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid
		Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type ad:LayoutAnchorablePane}}}"
		Height="{Binding Path=ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type ad:LayoutAnchorablePane}}}"
	>
		<DataGrid 
			ItemsSource="{Binding Modules}" 
			AutoGenerateColumns="False" 
			SelectionUnit="FullRow"
			CanUserReorderColumns="True"
			CanUserResizeColumns="True"
			CanUserSortColumns="True"
		>
			<DataGrid.Columns>
				<DataGridTextColumn Header="Address" IsReadOnly="True"></DataGridTextColumn>
				<DataGridTextColumn Header="Name" IsReadOnly="True"></DataGridTextColumn>
			</DataGrid.Columns>
		</DataGrid>   
    </Grid>
</UserControl>
